echo
----
{
  b := &kv.Batch{}
  b.Put(uint64ToKey(1), seq2val(kvnemesisutil.Seq(21))) // <nil>
  b.DelRange(uint64ToKey(2), uint64ToKey(3), true /* @s22 */) // <nil>
  db1.Run(ctx, b) // @<ts> <nil>
}
