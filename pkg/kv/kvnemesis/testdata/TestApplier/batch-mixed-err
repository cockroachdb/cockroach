echo
----
{
  b := &kv.Batch{}
  b.Put(uint64ToKey(2), seq2val(kvnemesisutil.Seq(2))) // context canceled
  b.GetForUpdate(uint64ToKey(1)) // context canceled
  b.ScanForUpdate(uint64ToKey(1), uint64ToKey(3)) // context canceled
  b.ReverseScan(uint64ToKey(1), uint64ToKey(3)) // context canceled
  db0.Run(ctx, b) // context canceled
}
