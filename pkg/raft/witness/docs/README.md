# raft/witness — documentation

- [`cost-details.md`](./cost-details.md) — leader CPU and network costs of
  raft replication; per-witness savings sweep across value sizes
  (256 B / 1 KiB / 4 KiB) with full reproduction recipe.
- [`site/`](./site/) — SvelteKit-based docs site (`cd site &&
  npm run dev`). Page sources live in `site/src/routes/(docs)/`.
