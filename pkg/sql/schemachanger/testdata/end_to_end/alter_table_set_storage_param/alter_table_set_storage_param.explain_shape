/* setup */
CREATE TABLE t (i int);
CREATE SCHEMA sc;

/* test */
EXPLAIN (DDL, SHAPE) ALTER TABLE t SET (exclude_data_from_backup=true);
----
Schema change plan for ALTER TABLE ‹defaultdb›.‹public›.‹t› SET ('exclude_data_from_backup' = ‹true›);
 └── execute 2 system table mutations transactions
