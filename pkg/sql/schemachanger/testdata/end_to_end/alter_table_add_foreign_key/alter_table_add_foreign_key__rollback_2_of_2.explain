/* setup */
CREATE TABLE t1 (i INT PRIMARY KEY);
CREATE TABLE t2 (i INT PRIMARY KEY);
INSERT INTO t2 (i) VALUES(-1), (-2), (-3);
INSERT INTO t1 (i) VALUES(-1), (-2), (-3);

/* test */
ALTER TABLE t1 ADD FOREIGN KEY (i) REFERENCES t2(i);
EXPLAIN (DDL) rollback at post-commit stage 2 of 2;
----
Schema change plan for rolling back ALTER TABLE ‹defaultdb›.public.‹t1› ADD CONSTRAINT ‹t1_i_fkey› FOREIGN KEY (‹i›) REFERENCES ‹defaultdb›.public.‹t2› (‹i›);
 └── PostCommitNonRevertiblePhase
      └── Stage 1 of 1 in PostCommitNonRevertiblePhase
           ├── 2 elements transitioning toward ABSENT
           │    ├── WRITE_ONLY → ABSENT ForeignKeyConstraint:{DescID: 10000 (t1), IndexID: 0, ConstraintID: 2 (t1_i_fkey-), ReferencedDescID: 10001 (t2)}
           │    └── PUBLIC     → ABSENT ConstraintWithoutIndexName:{DescID: 10000 (t1), Name: "t1_i_fkey", ConstraintID: 2 (t1_i_fkey-)}
           └── 6 Mutation operations
                ├── SetConstraintName {"ConstraintID":2,"Name":"crdb_internal_co...","TableID":10000}
                ├── RemoveForeignKeyBackReference {"OriginConstraintID":2,"OriginTableID":10000,"ReferencedTableID":10001}
                ├── RemoveForeignKeyConstraint {"ConstraintID":2,"TableID":10000}
                ├── RemoveJobStateFromDescriptor {"DescriptorID":10000}
                ├── RemoveJobStateFromDescriptor {"DescriptorID":10001}
                └── UpdateSchemaChangerJob {"IsNonCancelable":true,"RunningStatus":"all stages compl..."}
