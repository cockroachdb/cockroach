/* setup */
CREATE TABLE t1 (id INT PRIMARY KEY, data TEXT, expired_at TIMESTAMPTZ) WITH (ttl_expiration_expression='expired_at');

/* test */
EXPLAIN (DDL, SHAPE) ALTER TABLE t1 RENAME TO t1_renamed;
----
Schema change plan for ALTER TABLE ‹t1› RENAME TO ‹t1_renamed›;
 └── execute 1 system table mutations transaction
