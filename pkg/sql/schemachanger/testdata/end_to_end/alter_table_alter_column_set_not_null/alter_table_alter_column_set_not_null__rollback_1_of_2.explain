/* setup */
CREATE TABLE t (i INT PRIMARY KEY, j INT);

/* test */
ALTER TABLE t ALTER COLUMN j SET NOT NULL;
EXPLAIN (DDL) rollback at post-commit stage 1 of 2;
----
Schema change plan for rolling back ALTER TABLE ‹defaultdb›.public.‹t› ALTER COLUMN ‹j› SET NOT NULL;
 └── PostCommitNonRevertiblePhase
      └── Stage 1 of 1 in PostCommitNonRevertiblePhase
           ├── 1 element transitioning toward ABSENT
           │    └── WRITE_ONLY → ABSENT ColumnNotNull:{DescID: 10000 (t), ColumnID: 2 (j), IndexID: 0}
           └── 3 Mutation operations
                ├── RemoveColumnNotNull {"ColumnID":2,"TableID":10000}
                ├── RemoveJobStateFromDescriptor {"DescriptorID":10000}
                └── UpdateSchemaChangerJob {"IsNonCancelable":true,"RunningStatus":"all stages compl..."}
