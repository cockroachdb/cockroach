/* setup */
CREATE TABLE t (id INT PRIMARY KEY, created_at TIMESTAMPTZ) WITH (ttl_expiration_expression = 'created_at');

/* test */
EXPLAIN (DDL, SHAPE) ALTER TABLE t SET (ttl_select_rate_limit = 100, ttl_job_cron = '0 4 * * *');
----
Schema change plan for ALTER TABLE ‹defaultdb›.‹public›.‹t› SET ('ttl_select_rate_limit' = ‹100›, 'ttl_job_cron' = ‹'0 4 * * *'›);
 └── execute 2 system table mutations transactions
