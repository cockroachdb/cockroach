// Code generated by TestPretty. DO NOT EDIT.
// GENERATED FILE DO NOT EDIT
1:
-
CREATE INDEX foo_b_idx
	ON foo (
		b,
		c,
		d
	)
	PARTITION BY LIST (
		b
	)
		(
			PARTITION baz
				VALUES IN (
					'baz'
				),
			PARTITION "default"
				VALUES IN (
					DEFAULT
				)
		)

20:
--------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (
		b
	)
		(
			PARTITION baz
				VALUES IN (
					'baz'
				),
			PARTITION "default"
				VALUES IN (
					DEFAULT
				)
		)

23:
-----------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (
		b
	) (
		PARTITION baz
			VALUES IN (
				'baz'
			),
		PARTITION "default"
			VALUES IN (
				DEFAULT
			)
	  )

27:
---------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b) (
							PARTITION baz
								VALUES IN (
									'baz'
								),
							PARTITION "default"
								VALUES IN (
									DEFAULT
								)
	                      )

31:
-------------------------------
CREATE INDEX foo_b_idx ON foo (
						b,
						c,
						d
                       )
                       PARTITION BY LIST (
						b
                       )
						(
							PARTITION baz
								VALUES IN (
									'baz'
								),
							PARTITION "default"
								VALUES IN (
									DEFAULT
								)
						)

39:
---------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (
						b
                       )
						(
							PARTITION baz
								VALUES IN (
									'baz'
								),
							PARTITION "default"
								VALUES IN (
									DEFAULT
								)
						)

42:
------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (
						b
                       ) (
							PARTITION baz
								VALUES IN (
									'baz'
								),
							PARTITION "default"
								VALUES IN (
									DEFAULT
								)
                         )

46:
----------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz
													VALUES IN (
														'baz'
													),
												PARTITION "default"
													VALUES IN (
														DEFAULT
													)
                                             )

70:
----------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz
													VALUES IN ('baz'),
												PARTITION "default"
													VALUES IN (
														DEFAULT
													)
                                             )

71:
-----------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz
													VALUES IN ('baz'),
												PARTITION "default"
													VALUES IN (DEFAULT)
                                             )

73:
-------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz VALUES IN (
																'baz'
												              ),
												PARTITION "default"
													VALUES IN (DEFAULT)
                                             )

79:
-------------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz VALUES IN (
																'baz'
												              ),
												PARTITION "default" VALUES IN (
																		DEFAULT
												                    )
                                             )

80:
--------------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz VALUES IN ('baz'),
												PARTITION "default" VALUES IN (
																		DEFAULT
												                    )
                                             )

87:
---------------------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (
												PARTITION baz VALUES IN ('baz'),
												PARTITION "default" VALUES IN (DEFAULT)
                                             )

119:
-----------------------------------------------------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d)
                       PARTITION BY LIST (b) (PARTITION baz VALUES IN ('baz'), PARTITION "default" VALUES IN (DEFAULT))

136:
----------------------------------------------------------------------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d) PARTITION BY LIST (b) (PARTITION baz VALUES IN ('baz'), PARTITION "default" VALUES IN (DEFAULT))


