// Code generated by TestPretty. DO NOT EDIT.
// GENERATED FILE DO NOT EDIT
1:
-
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount
			<= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance
					= balance
					- amount
			WHERE
				id
				= from_id;
			UPDATE
				accounts
			SET
				balance
					= balance
					+ amount
			WHERE
				id
				= to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE
						'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

27:
---------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount
			<= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance
					= balance
					- amount
			WHERE
				id
				= from_id;
			UPDATE
				accounts
			SET
				balance
					= balance
					+ amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE
						'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

29:
-----------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount
			<= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance
					= balance
					- amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance
					= balance
					+ amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE
						'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

31:
-------------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance
					= balance
					- amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance
					= balance
					+ amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE
						'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

38:
--------------------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance
					= balance - amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance
					= balance + amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE
						'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

40:
----------------------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance
					= balance - amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance
					= balance + amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

42:
------------------------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance = balance - amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance = balance + amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE
								'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

49:
-------------------------------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION
					'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance = balance - amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance = balance + amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE 'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

58:
----------------------------------------------------------
CREATE PROCEDURE transfer(
	from_id INT8,
	to_id INT8,
	amount DECIMAL
)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION 'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance = balance - amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance = balance + amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE 'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

67:
-------------------------------------------------------------------
CREATE PROCEDURE transfer(from_id INT8, to_id INT8, amount DECIMAL)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION 'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance = balance - amount
			WHERE
				id = from_id;
			UPDATE
				accounts
			SET
				balance = balance + amount
			WHERE
				id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE 'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

76:
----------------------------------------------------------------------------
CREATE PROCEDURE transfer(from_id INT8, to_id INT8, amount DECIMAL)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION 'amount must be positive';
			END IF;
			UPDATE
				accounts
			SET
				balance = balance - amount
			WHERE
				id = from_id;
			UPDATE accounts SET balance = balance + amount WHERE id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE 'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$

78:
------------------------------------------------------------------------------
CREATE PROCEDURE transfer(from_id INT8, to_id INT8, amount DECIMAL)
	LANGUAGE plpgsql
	AS $funcbodyx$
		BEGIN
			IF amount <= 0 THEN
				RAISE EXCEPTION 'amount must be positive';
			END IF;
			UPDATE accounts SET balance = balance - amount WHERE id = from_id;
			UPDATE accounts SET balance = balance + amount WHERE id = to_id;
			DO $funcbody$
				BEGIN
					RAISE NOTICE 'done';
					DO $$
						BEGIN
							RAISE NOTICE 'inner';
						END;
					$$;
				END;
			$funcbody$;
		END;
	$funcbodyx$


