// Code generated by TestPretty. DO NOT EDIT.
// GENERATED FILE DO NOT EDIT
1:
-
SELECT
	sum(
		a
	) OVER w,
	avg(
		b
	) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
	),
	min(
		f
	) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
	)

17:
-----------------
SELECT
	sum(
		a
	) OVER w,
	avg(b) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
	),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
	)

18:
------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
	),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
	)

47:
-----------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
	),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN j PRECEDING AND CURRENT ROW
	)

50:
--------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (
		PARTITION BY c ORDER BY d ROWS e PRECEDING
	),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN j PRECEDING AND CURRENT ROW
	)

55:
-------------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (
		PARTITION BY c ORDER BY d ROWS e PRECEDING
	),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN j PRECEDING AND CURRENT ROW
	)

61:
-------------------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN j PRECEDING AND CURRENT ROW
	)

65:
-----------------------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW
	)

68:
--------------------------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
	min(f) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

81:
---------------------------------------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
	min(f) OVER (
		ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

91:
-------------------------------------------------------------------------------------------
SELECT
	sum(a) OVER w,
	avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
	min(f) OVER (ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES)
FROM
	t
WINDOW
	w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

164:
--------------------------------------------------------------------------------------------------------------------------------------------------------------------
SELECT
	sum(a) OVER w, avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING), min(f) OVER (ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES)
FROM
	t
WINDOW
	w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

246:
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
SELECT sum(a) OVER w, avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING), min(f) OVER (ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES) FROM t WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)


