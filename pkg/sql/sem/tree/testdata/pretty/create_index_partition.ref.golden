// Code generated by TestPretty. DO NOT EDIT.
// GENERATED FILE DO NOT EDIT
1:
-
CREATE INDEX foo_b_idx
	ON foo (
		b,
		c,
		d
	)
	PARTITION BY LIST (
		b
	)
		(
			PARTITION baz
				VALUES IN (
					'baz'
				),
			PARTITION "default"
				VALUES IN (
					DEFAULT
				)
		)

20:
--------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (
		b
	)
		(
			PARTITION baz
				VALUES IN (
					'baz'
				),
			PARTITION "default"
				VALUES IN (
					DEFAULT
				)
		)

25:
-------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b)
		(
			PARTITION baz
				VALUES IN (
					'baz'
				),
			PARTITION "default"
				VALUES IN (
					DEFAULT
				)
		)

34:
----------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b)
		(
			PARTITION baz
				VALUES IN ('baz'),
			PARTITION "default"
				VALUES IN (
					DEFAULT
				)
		)

35:
-----------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b)
		(
			PARTITION baz
				VALUES IN ('baz'),
			PARTITION "default"
				VALUES IN (DEFAULT)
		)

44:
--------------------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b)
		(
			PARTITION baz VALUES IN ('baz'),
			PARTITION "default"
				VALUES IN (DEFAULT)
		)

51:
---------------------------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b)
		(
			PARTITION baz VALUES IN ('baz'),
			PARTITION "default" VALUES IN (DEFAULT)
		)

82:
----------------------------------------------------------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b)
		(PARTITION baz VALUES IN ('baz'), PARTITION "default" VALUES IN (DEFAULT))

100:
----------------------------------------------------------------------------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d)
	PARTITION BY LIST (b) (PARTITION baz VALUES IN ('baz'), PARTITION "default" VALUES IN (DEFAULT))

117:
---------------------------------------------------------------------------------------------------------------------
CREATE INDEX foo_b_idx
	ON foo (b, c, d) PARTITION BY LIST (b) (PARTITION baz VALUES IN ('baz'), PARTITION "default" VALUES IN (DEFAULT))

136:
----------------------------------------------------------------------------------------------------------------------------------------
CREATE INDEX foo_b_idx ON foo (b, c, d) PARTITION BY LIST (b) (PARTITION baz VALUES IN ('baz'), PARTITION "default" VALUES IN (DEFAULT))


