// Code generated by TestPretty. DO NOT EDIT.
// GENERATED FILE DO NOT EDIT
1:
-
SELECT
	sum(
		a
	) OVER w,
	avg(
		b
	) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
	),
	min(
		f
	) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
	)
FROM
	t
WINDOW
	w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
	)

11:
-----------
SELECT sum(
		a
       ) OVER w,
       avg(
		b
       ) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
       ),
       min(
		f
       ) OVER (
		ORDER BY
			g
		RANGE
			BETWEEN
				UNBOUNDED PRECEDING
			AND
				h FOLLOWING
			EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
       )

18:
------------------
SELECT sum(
		a
       ) OVER w,
       avg(
		b
       ) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
       ),
       min(
		f
       ) OVER (
		ORDER BY g
		   RANGE BETWEEN
					UNBOUNDED PRECEDING
		         AND
					h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
       )

20:
--------------------
SELECT sum(
		a
       ) OVER w,
       avg(b) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN
					UNBOUNDED PRECEDING
		         AND
					h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
       )

21:
---------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY
			c
		ORDER BY
			d
		ROWS
			e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN
					UNBOUNDED PRECEDING
		         AND
					h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY
			i
		GROUPS
			BETWEEN
				j PRECEDING
			AND
				CURRENT ROW
       )

22:
----------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY c
		    ORDER BY d
		        ROWS e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN
					UNBOUNDED PRECEDING
		         AND
					h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN
						j PRECEDING
		             AND
						CURRENT ROW
       )

40:
----------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY c
		    ORDER BY d
		        ROWS e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN
					UNBOUNDED PRECEDING
		         AND
					h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN j PRECEDING
		                 AND CURRENT ROW
       )

44:
--------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY c
		    ORDER BY d
		        ROWS e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING
		             AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN j PRECEDING
		                 AND CURRENT ROW
       )

50:
--------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY c ORDER BY d ROWS e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING
		             AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN j PRECEDING
		                 AND CURRENT ROW
       )

56:
--------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY c ORDER BY d ROWS e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING
		             AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN j PRECEDING AND CURRENT ROW
       )

60:
------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (
		PARTITION BY c ORDER BY d ROWS e PRECEDING
       ),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN j PRECEDING AND CURRENT ROW
       )

64:
----------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i
		      GROUPS BETWEEN j PRECEDING AND CURRENT ROW
       )

65:
-----------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (
		PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW
       )

71:
-----------------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING
		         EXCLUDE TIES
       )
  FROM t
WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

73:
-------------------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
       min(f) OVER (
		ORDER BY g
		   RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES
       )
  FROM t
WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

81:
---------------------------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
       min(f) OVER (
		ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES
       )
  FROM t
WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

94:
----------------------------------------------------------------------------------------------
SELECT sum(a) OVER w,
       avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING),
       min(f) OVER (ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES)
  FROM t
WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

167:
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
SELECT sum(a) OVER w, avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING), min(f) OVER (ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES)
  FROM t
WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)

246:
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
SELECT sum(a) OVER w, avg(b) OVER (PARTITION BY c ORDER BY d ROWS e PRECEDING), min(f) OVER (ORDER BY g RANGE BETWEEN UNBOUNDED PRECEDING AND h FOLLOWING EXCLUDE TIES) FROM t WINDOW w AS (PARTITION BY i GROUPS BETWEEN j PRECEDING AND CURRENT ROW)


