// Code generated by TestPretty. DO NOT EDIT.
// GENERATED FILE DO NOT EDIT
1:
-
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id,
						amount
					FROM
						orders
					WHERE
						amount
						> min_amount
						AND status
							= 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount
				DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE
					'queried';
				DO $$
					BEGIN
						RAISE NOTICE
							'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

23:
-----------------------
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id,
						amount
					FROM
						orders
					WHERE
						amount
						> min_amount
						AND status
							= 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE
					'queried';
				DO $$
					BEGIN
						RAISE NOTICE
							'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

34:
----------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id, amount
					FROM
						orders
					WHERE
						amount
						> min_amount
						AND status
							= 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE
					'queried';
				DO $$
					BEGIN
						RAISE NOTICE
							'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

39:
---------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id, amount
					FROM
						orders
					WHERE
						amount
						> min_amount
						AND status
							= 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE
							'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

43:
-------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id, amount
					FROM
						orders
					WHERE
						amount > min_amount
						AND status
							= 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE
							'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

45:
---------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id, amount
					FROM
						orders
					WHERE
						amount > min_amount
						AND status = 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

65:
-----------------------------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(
	min_amount DECIMAL,
	lim INT8
)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id, amount
					FROM
						orders
					WHERE
						amount > min_amount AND status = 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

71:
-----------------------------------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(min_amount DECIMAL, lim INT8)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT
						id, amount
					FROM
						orders
					WHERE
						amount > min_amount AND status = 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

97:
-------------------------------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(min_amount DECIMAL, lim INT8)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (
					SELECT id, amount FROM orders WHERE amount > min_amount AND status = 'active'
				)
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

98:
--------------------------------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(min_amount DECIMAL, lim INT8)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered
				AS (SELECT id, amount FROM orders WHERE amount > min_amount AND status = 'active')
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

103:
-------------------------------------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(min_amount DECIMAL, lim INT8)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH
			filtered AS (SELECT id, amount FROM orders WHERE amount > min_amount AND status = 'active')
		SELECT
			id
		FROM
			filtered
		ORDER BY
			amount DESC
		LIMIT
			lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$

160:
----------------------------------------------------------------------------------------------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION get_top_orders(min_amount DECIMAL, lim INT8)
	RETURNS SETOF INT8
	STABLE
	LANGUAGE SQL
	AS $funcbodyx$
		WITH filtered AS (SELECT id, amount FROM orders WHERE amount > min_amount AND status = 'active') SELECT id FROM filtered ORDER BY amount DESC LIMIT lim;
		DO $funcbody$
			BEGIN
				RAISE NOTICE 'queried';
				DO $$
					BEGIN
						RAISE NOTICE 'inner';
					END;
				$$;
			END;
		$funcbody$;
	$funcbodyx$


