exec-ddl
CREATE TABLE abc (a INT PRIMARY KEY, b INT, c INT, INDEX abc_b_idx (b), INDEX abc_c_idx (c))
----

# --------------------------------------------------
# Exact match — no penalty.
# --------------------------------------------------

# PlanGram matches the plan the optimizer would choose anyway.
opt format=show-cost plangram=(root: (Scan);)
SELECT * FROM abc
----
scan abc
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1088.62
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 └── fd: (1)-->(2,3)

# --------------------------------------------------
# Mismatch — penalty applied.
# --------------------------------------------------

# PlanGram specifies an operator that doesn't match.
opt format=show-cost plangram=(root: (Select);)
SELECT * FROM abc
----
scan abc
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1088.62
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (1)
 └── fd: (1)-->(2,3)

# --------------------------------------------------
# Matching with specific index.
# --------------------------------------------------

# PlanGram constraining which index to use.
opt format=show-cost plangram=(root: (Select (IndexJoin (Scan)));)
SELECT * FROM abc WHERE b = 1 AND c = 1
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# --------------------------------------------------
# Alternates — optimizer picks best matching alternate.
# --------------------------------------------------

# PlanGram with multiple index alternates via a production.
opt format=show-cost plangram=(root: (Select (IndexJoin scan)); scan: (Scan Index="abc_b_idx") | (Scan Index="abc_c_idx");)
SELECT * FROM abc WHERE b = 1 AND c = 1
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# --------------------------------------------------
# Sort visibility — matching.
# --------------------------------------------------

# PlanGram that explicitly includes Sort matches a plan with Sort.
opt format=show-cost plangram=(root: (Sort (Scan));)
SELECT * FROM abc ORDER BY b
----
sort
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1338.10194
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: (1)-->(2,3)
 ├── ordering: +2
 └── scan abc
      ├── columns: a:1!null b:2 c:3
      ├── cost: 1088.62
      ├── cost-flags: unbounded-cardinality
      ├── key: (1)
      └── fd: (1)-->(2,3)

# --------------------------------------------------
# Sort visibility — mismatch.
# --------------------------------------------------

# PlanGram without Sort applied to a query that needs Sort.
opt format=show-cost plangram=(root: (Scan);)
SELECT * FROM abc ORDER BY b
----
sort
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1338.10194
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (1)
 ├── fd: (1)-->(2,3)
 ├── ordering: +2
 └── scan abc
      ├── columns: a:1!null b:2 c:3
      ├── cost: 1088.62
      ├── cost-flags: plangram-mismatch unbounded-cardinality
      ├── key: (1)
      └── fd: (1)-->(2,3)

# --------------------------------------------------
# WithNoneFallback — graceful fallback.
# --------------------------------------------------

# Default plan for comparison.
opt format=show-cost
SELECT * FROM abc WHERE b = 1 AND c = 1
----
inner-join (zigzag abc@abc_b_idx abc@abc_c_idx)
 ├── columns: a:1!null b:2!null c:3!null
 ├── eq columns: [1] = [1]
 ├── left fixed columns: [2] = [1]
 ├── right fixed columns: [3] = [1]
 ├── cost: 30.14
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 └── filters
      ├── b:2 = 1 [outer=(2), fd=()-->(2)]
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# Impossible PlanGram — should get same plan shape but with penalty.
opt format=show-cost plangram=(root: (InnerJoin);)
SELECT * FROM abc WHERE b = 1 AND c = 1
----
inner-join (zigzag abc@abc_b_idx abc@abc_c_idx)
 ├── columns: a:1!null b:2!null c:3!null
 ├── eq columns: [1] = [1]
 ├── left fixed columns: [2] = [1]
 ├── right fixed columns: [3] = [1]
 ├── cost: 30.14
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 └── filters
      ├── b:2 = 1 [outer=(2), fd=()-->(2)]
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# --------------------------------------------------
# Child count flexibility — fewer children specified.
# --------------------------------------------------

# PlanGram with fewer children than the expression has. Should still match.
opt format=show-cost plangram=(root: (Select (Scan));)
SELECT * FROM abc WHERE a = 1
----
select
 ├── columns: a:1!null b:2 c:3
 ├── cardinality: [0 - 1]
 ├── cost: 1098.64
 ├── cost-flags: unbounded-cardinality
 ├── key: ()
 ├── fd: ()-->(1-3)
 ├── scan abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    └── fd: (1)-->(2,3)
 └── filters
      └── a:1 = 1 [outer=(1), fd=()-->(1)]

# --------------------------------------------------
# Nested multi-level matching.
# --------------------------------------------------

# Deep tree: Select → IndexJoin → Scan, all matching. Use a filter on c that
# can't be pushed into the b-index scan, forcing Select to remain.
opt format=show-cost plangram=(root: (Select (IndexJoin (Scan)));)
SELECT * FROM abc WHERE b = 1 AND c > 0
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2), (1)-->(3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 > 0 [outer=(3)]

# --------------------------------------------------
# Partial tree mismatch cascading.
# --------------------------------------------------

# PlanGram that matches Select at top but specifies Sort as the child, which
# mismatches the actual IndexJoin child. The mismatch penalty cascades through
# the subtree below the mismatch point.
opt format=show-cost plangram=(root: (Select (Sort));)
SELECT * FROM abc WHERE b = 1 AND c > 0
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2), (1)-->(3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: plangram-mismatch unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 > 0 [outer=(3)]

# --------------------------------------------------
# AnyPlanGram for unspecified children.
# --------------------------------------------------

# PlanGram specifying only the root operator, leaving children unconstrained.
opt format=show-cost plangram=(root: (Select);)
SELECT * FROM abc WHERE a = 1
----
select
 ├── columns: a:1!null b:2 c:3
 ├── cardinality: [0 - 1]
 ├── cost: 1098.64
 ├── cost-flags: unbounded-cardinality
 ├── key: ()
 ├── fd: ()-->(1-3)
 ├── scan abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    └── fd: (1)-->(2,3)
 └── filters
      └── a:1 = 1 [outer=(1), fd=()-->(1)]

# --------------------------------------------------
# Invisible operator transparency — Distribute.
# --------------------------------------------------

exec-ddl
ALTER TABLE abc CONFIGURE ZONE USING constraints='[+region=us]'
----

# When a DistributeExpr wraps the plan, the PlanGram should see through it
# and match the inner Scan without penalty.
opt format=show-cost locality=(region=eu) plangram=(root: (Scan);)
SELECT * FROM abc
----
distribute
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1545.24
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: (1)-->(2,3)
 ├── distribution: eu
 ├── input distribution: us
 └── scan abc
      ├── columns: a:1!null b:2 c:3
      ├── cost: 1345.22
      ├── cost-flags: unbounded-cardinality
      ├── key: (1)
      └── fd: (1)-->(2,3)

exec-ddl
ALTER TABLE abc CONFIGURE ZONE USING DEFAULT
----

# --------------------------------------------------
# Sort + PlanGram interaction — Sort at root.
# --------------------------------------------------

# PlanGram with Sort at root matches a query that requires sorting.
opt format=show-cost plangram=(root: (Sort (Scan));)
SELECT * FROM abc ORDER BY b
----
sort
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1338.10194
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: (1)-->(2,3)
 ├── ordering: +2
 └── scan abc
      ├── columns: a:1!null b:2 c:3
      ├── cost: 1088.62
      ├── cost-flags: unbounded-cardinality
      ├── key: (1)
      └── fd: (1)-->(2,3)

# --------------------------------------------------
# Alternates — PlanGram steers to non-default plan shape.
# --------------------------------------------------

# The default plan for this query uses a zigzag join. The PlanGram specifies
# Select → IndexJoin → Scan, which is a different operator shape. This proves
# the PlanGram steered the optimizer to a non-default plan.
opt format=show-cost plangram=(root: (Select (IndexJoin (Scan)));)
SELECT * FROM abc WHERE b = 1 AND c = 1
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# --------------------------------------------------
# WithNoneFallback — PlanGram preference wins over default.
# --------------------------------------------------

# The default plan uses a zigzag join (cost ~30), but the PlanGram specifies
# Select → IndexJoin → Scan. WithNoneFallback ensures both paths are
# considered, and the PlanGram preference wins because the non-matching
# (zigzag) plan gets a mismatch penalty.
opt format=show-cost plangram=(root: (Select (IndexJoin (Scan)));)
SELECT * FROM abc WHERE b = 1 AND c = 1
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# ============================================================
# Additional tables for multi-table tests.
# ============================================================

exec-ddl
CREATE TABLE xy (x INT PRIMARY KEY, y INT, INDEX xy_y_idx (y))
----

# --------------------------------------------------
# Join — InnerJoin match.
# --------------------------------------------------

opt format=show-cost plangram=(root: (InnerJoin (Scan) (Scan));)
SELECT * FROM abc, xy WHERE a = x
----
inner-join (hash)
 ├── columns: a:1!null b:2 c:3 x:6!null y:7
 ├── multiplicity: left-rows(zero-or-one), right-rows(zero-or-one)
 ├── cost: 2197.20625
 ├── cost-flags: unbounded-cardinality
 ├── key: (6)
 ├── fd: (1)-->(2,3), (6)-->(7), (1)==(6), (6)==(1)
 ├── scan abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    └── fd: (1)-->(2,3)
 ├── scan xy
 │    ├── columns: x:6!null y:7
 │    ├── cost: 1068.42
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (6)
 │    └── fd: (6)-->(7)
 └── filters
      └── a:1 = x:6 [outer=(1,6), fd=(1)==(6), (6)==(1)]

# --------------------------------------------------
# Join — type mismatch (LeftJoin vs InnerJoin).
# --------------------------------------------------

opt format=show-cost plangram=(root: (LeftJoin (Scan) (Scan));)
SELECT * FROM abc, xy WHERE a = x
----
inner-join (merge)
 ├── columns: a:1!null b:2 c:3 x:6!null y:7
 ├── left ordering: +1
 ├── right ordering: +6
 ├── cost: 2187.06
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (6)
 ├── fd: (1)-->(2,3), (6)-->(7), (1)==(6), (6)==(1)
 ├── scan abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: (1)-->(2,3)
 │    └── ordering: +1
 ├── scan xy
 │    ├── columns: x:6!null y:7
 │    ├── cost: 1068.42
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    ├── key: (6)
 │    ├── fd: (6)-->(7)
 │    └── ordering: +6
 └── filters (true)

# --------------------------------------------------
# Join — mixed child match/mismatch.
# --------------------------------------------------

opt format=show-cost plangram=(root: (InnerJoin (Select) (Scan));)
SELECT * FROM abc, xy WHERE a = x
----
inner-join (merge)
 ├── columns: a:1!null b:2 c:3 x:6!null y:7
 ├── left ordering: +1
 ├── right ordering: +6
 ├── cost: 2187.06
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (6)
 ├── fd: (1)-->(2,3), (6)-->(7), (1)==(6), (6)==(1)
 ├── scan abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: (1)-->(2,3)
 │    └── ordering: +1
 ├── scan xy
 │    ├── columns: x:6!null y:7
 │    ├── cost: 1068.42
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    ├── key: (6)
 │    ├── fd: (6)-->(7)
 │    └── ordering: +6
 └── filters (true)

# --------------------------------------------------
# Join — steering to LookupJoin.
# --------------------------------------------------

opt format=show-cost plangram=(root: (LookupJoin (Scan));)
SELECT * FROM abc, xy WHERE a = x
----
inner-join (lookup abc)
 ├── columns: a:1!null b:2 c:3 x:6!null y:7
 ├── key columns: [6] = [1]
 ├── lookup columns are key
 ├── cost: 7142.44
 ├── cost-flags: unbounded-cardinality
 ├── key: (6)
 ├── fd: (1)-->(2,3), (6)-->(7), (1)==(6), (6)==(1)
 ├── scan xy
 │    ├── columns: x:6!null y:7
 │    ├── cost: 1068.42
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (6)
 │    └── fd: (6)-->(7)
 └── filters (true)

# --------------------------------------------------
# Join — steering to MergeJoin.
# --------------------------------------------------

opt format=show-cost plangram=(root: (MergeJoin (Scan) (Scan));)
SELECT * FROM abc, xy WHERE a = x
----
inner-join (merge)
 ├── columns: a:1!null b:2 c:3 x:6!null y:7
 ├── left ordering: +1
 ├── right ordering: +6
 ├── cost: 2187.06
 ├── cost-flags: unbounded-cardinality
 ├── key: (6)
 ├── fd: (1)-->(2,3), (6)-->(7), (1)==(6), (6)==(1)
 ├── scan abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: (1)-->(2,3)
 │    └── ordering: +1
 ├── scan xy
 │    ├── columns: x:6!null y:7
 │    ├── cost: 1068.42
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (6)
 │    ├── fd: (6)-->(7)
 │    └── ordering: +6
 └── filters (true)

# --------------------------------------------------
# Aggregation — GroupBy match.
# --------------------------------------------------

opt format=show-cost plangram=(root: (GroupBy (Scan));)
SELECT b, count(*) FROM abc GROUP BY b
----
group-by (streaming)
 ├── columns: b:2 count:6!null
 ├── grouping columns: b:2
 ├── internal-ordering: +2
 ├── cost: 1079.35
 ├── cost-flags: unbounded-cardinality
 ├── key: (2)
 ├── fd: (2)-->(6)
 ├── scan abc@abc_b_idx
 │    ├── columns: b:2
 │    ├── cost: 1058.32
 │    ├── cost-flags: unbounded-cardinality
 │    └── ordering: +2
 └── aggregations
      └── count-rows [as=count_rows:6]

# --------------------------------------------------
# Aggregation — ScalarGroupBy match.
# --------------------------------------------------

opt format=show-cost plangram=(root: (ScalarGroupBy (Scan));)
SELECT count(*) FROM abc
----
scalar-group-by
 ├── columns: count:6!null
 ├── cardinality: [1 - 1]
 ├── cost: 1058.25
 ├── cost-flags: unbounded-cardinality
 ├── key: ()
 ├── fd: ()-->(6)
 ├── scan abc@abc_b_idx
 │    ├── cost: 1048.22
 │    └── cost-flags: unbounded-cardinality
 └── aggregations
      └── count-rows [as=count_rows:6]

# --------------------------------------------------
# Aggregation — GroupBy vs ScalarGroupBy mismatch.
# --------------------------------------------------

opt format=show-cost plangram=(root: (GroupBy (Scan));)
SELECT count(*) FROM abc
----
scalar-group-by
 ├── columns: count:6!null
 ├── cardinality: [1 - 1]
 ├── cost: 1058.25
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: ()
 ├── fd: ()-->(6)
 ├── scan abc@abc_b_idx
 │    ├── cost: 1048.22
 │    └── cost-flags: plangram-mismatch unbounded-cardinality
 └── aggregations
      └── count-rows [as=count_rows:6]

# --------------------------------------------------
# Limit + Sort PlanGram.
# --------------------------------------------------

opt format=show-cost plangram=(root: (Limit (Sort (Scan)));)
SELECT * FROM abc ORDER BY b LIMIT 5
----
limit
 ├── columns: a:1!null b:2 c:3
 ├── internal-ordering: +2
 ├── cardinality: [0 - 5]
 ├── cost: 1338.16194
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: (1)-->(2,3)
 ├── ordering: +2
 ├── sort
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 1338.10194
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: (1)-->(2,3)
 │    ├── ordering: +2
 │    ├── limit hint: 5.00
 │    └── scan abc
 │         ├── columns: a:1!null b:2 c:3
 │         ├── cost: 1088.62
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: (1)-->(2,3)
 └── 5

# --------------------------------------------------
# TopK PlanGram.
# --------------------------------------------------

opt format=show-cost plangram=(root: (TopK (Scan));)
SELECT * FROM abc ORDER BY b LIMIT 5
----
top-k
 ├── columns: a:1!null b:2 c:3
 ├── internal-ordering: +2
 ├── k: 5
 ├── cardinality: [0 - 5]
 ├── cost: 1155.21856
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: (1)-->(2,3)
 ├── ordering: +2
 └── scan abc
      ├── columns: a:1!null b:2 c:3
      ├── cost: 1088.62
      ├── cost-flags: unbounded-cardinality
      ├── key: (1)
      └── fd: (1)-->(2,3)

# --------------------------------------------------
# UnionAll match.
# --------------------------------------------------

opt format=show-cost plangram=(root: (UnionAll (Scan) (Scan));)
SELECT a FROM abc UNION ALL SELECT x FROM xy
----
union-all
 ├── columns: a:10!null
 ├── left columns: abc.a:1
 ├── right columns: x:6
 ├── cost: 2136.66
 ├── cost-flags: unbounded-cardinality
 ├── scan abc@abc_b_idx
 │    ├── columns: abc.a:1!null
 │    ├── cost: 1058.32
 │    ├── cost-flags: unbounded-cardinality
 │    └── key: (1)
 └── scan xy
      ├── columns: x:6!null
      ├── cost: 1058.32
      ├── cost-flags: unbounded-cardinality
      └── key: (6)

# --------------------------------------------------
# UnionAll mismatch (Union vs UnionAll).
# --------------------------------------------------

opt format=show-cost plangram=(root: (Union (Scan) (Scan));)
SELECT a FROM abc UNION ALL SELECT x FROM xy
----
union-all
 ├── columns: a:10!null
 ├── left columns: abc.a:1
 ├── right columns: x:6
 ├── cost: 2136.66
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── scan abc@abc_b_idx
 │    ├── columns: abc.a:1!null
 │    ├── cost: 1058.32
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    └── key: (1)
 └── scan xy
      ├── columns: x:6!null
      ├── cost: 1058.32
      ├── cost-flags: plangram-mismatch unbounded-cardinality
      └── key: (6)

# --------------------------------------------------
# Plan steering — default zigzag vs forced index scan.
# --------------------------------------------------

# Default plan for reference (zigzag join).
opt format=show-cost
SELECT * FROM abc WHERE b = 1 AND c = 1
----
inner-join (zigzag abc@abc_b_idx abc@abc_c_idx)
 ├── columns: a:1!null b:2!null c:3!null
 ├── eq columns: [1] = [1]
 ├── left fixed columns: [2] = [1]
 ├── right fixed columns: [3] = [1]
 ├── cost: 30.14
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 └── filters
      ├── b:2 = 1 [outer=(2), fd=()-->(2)]
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# Steer to Select → IndexJoin → Scan (non-default).
opt format=show-cost plangram=(root: (Select (IndexJoin (Scan)));)
SELECT * FROM abc WHERE b = 1 AND c = 1
----
select
 ├── columns: a:1!null b:2!null c:3!null
 ├── cost: 89.0700007
 ├── cost-flags: unbounded-cardinality
 ├── key: (1)
 ├── fd: ()-->(2,3)
 ├── index-join abc
 │    ├── columns: a:1!null b:2 c:3
 │    ├── cost: 88.9400007
 │    ├── cost-flags: unbounded-cardinality
 │    ├── key: (1)
 │    ├── fd: ()-->(2), (1)-->(3)
 │    └── scan abc@abc_b_idx
 │         ├── columns: a:1!null b:2!null
 │         ├── constraint: /2/1: [/1 - /1]
 │         ├── cost: 28.4200001
 │         ├── cost-flags: unbounded-cardinality
 │         ├── key: (1)
 │         └── fd: ()-->(2)
 └── filters
      └── c:3 = 1 [outer=(3), fd=()-->(3)]

# --------------------------------------------------
# CTE that gets inlined.
# --------------------------------------------------

opt format=show-cost plangram=(root: (Scan);)
WITH cte AS (SELECT * FROM abc) SELECT * FROM cte
----
project
 ├── columns: a:6!null b:7 c:8
 ├── cost: 1128.64
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (6)
 ├── fd: (6)-->(7,8)
 ├── scan abc
 │    ├── columns: abc.a:1!null abc.b:2 abc.c:3
 │    ├── cost: 1088.62
 │    ├── cost-flags: plangram-mismatch unbounded-cardinality
 │    ├── key: (1)
 │    └── fd: (1)-->(2,3)
 └── projections
      ├── abc.a:1 [as=a:6, outer=(1)]
      ├── abc.b:2 [as=b:7, outer=(2)]
      └── abc.c:3 [as=c:8, outer=(3)]

# --------------------------------------------------
# Mutation — INSERT with SELECT subtree.
# --------------------------------------------------

opt format=show-cost plangram=(root: (Insert (Select (Scan)));)
INSERT INTO xy SELECT a, b FROM abc WHERE a > 10
----
insert xy
 ├── columns: <none>
 ├── insert-mapping:
 │    ├── a:5 => x:1
 │    └── b:6 => y:2
 ├── cardinality: [0 - 0]
 ├── volatile, mutations
 ├── cost: 1078.46
 ├── cost-flags: unbounded-cardinality
 └── select
      ├── columns: a:5!null b:6
      ├── cost: 1078.45
      ├── cost-flags: unbounded-cardinality
      ├── key: (5)
      ├── fd: (5)-->(6)
      ├── scan abc@abc_b_idx
      │    ├── columns: a:5!null b:6
      │    ├── cost: 1068.42
      │    ├── cost-flags: unbounded-cardinality
      │    ├── key: (5)
      │    └── fd: (5)-->(6)
      └── filters
           └── a:5 > 10 [outer=(5)]

# --------------------------------------------------
# Extra children mismatch — PlanGram has more children than expr.
# --------------------------------------------------

opt format=show-cost plangram=(root: (Scan (Scan));)
SELECT * FROM abc
----
scan abc
 ├── columns: a:1!null b:2 c:3
 ├── cost: 1088.62
 ├── cost-flags: plangram-mismatch unbounded-cardinality
 ├── key: (1)
 └── fd: (1)-->(2,3)
