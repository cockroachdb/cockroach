exec-ddl
CREATE TABLE a (k INT PRIMARY KEY, i INT, f FLOAT, d DECIMAL, t TIME)
----

# --------------------------------------------------
# FoldPlusZero, FoldZeroPlus
# --------------------------------------------------

# Add columns to prevent NormalizeVar from swapping left and right.
norm expect=(FoldPlusZero,FoldZeroPlus)
SELECT
    (a.i + a.i) + 0 AS r, 0 + (a.i + a.i) AS s,
    (a.f + a.f) + 0 AS t, 0 + (a.f + a.f) AS u,
    (a.d + a.d) + 0 AS v, 0 + (a.d + a.d) AS w
FROM a
----
project
 ├── columns: r:6 s:7 t:8 u:9 v:10 w:11
 ├── scan a
 │    └── columns: i:2 f:3 d:4
 └── projections
      ├── i + i [outer=(2)]
      ├── i + i [outer=(2)]
      ├── f + f [outer=(3)]
      ├── f + f [outer=(3)]
      ├── d + d [outer=(4)]
      └── d + d [outer=(4)]


# Regression test for #35113.
norm expect=FoldPlusZero
SELECT i + 0::decimal FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

norm expect=FoldZeroPlus
SELECT 0::decimal + i FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

# --------------------------------------------------
# FoldMinusZero
# --------------------------------------------------

# Add columns to prevent NormalizeVar from swapping left and right.
norm expect=FoldMinusZero
SELECT
    (a.i + a.i) - 0 AS r,
    (a.f + a.f) - 0 AS s,
    (a.d + a.d) - 0 AS t
FROM a
----
project
 ├── columns: r:6 s:7 t:8
 ├── scan a
 │    └── columns: i:2 f:3 d:4
 └── projections
      ├── i + i [outer=(2)]
      ├── f + f [outer=(3)]
      └── d + d [outer=(4)]

# Regression test for #35113.
norm expect=FoldMinusZero
SELECT i - 0::decimal FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

# Regression test for #35612.
norm expect-not=FoldMinusZero
SELECT '[123]'::jsonb - 0
----
values
 ├── columns: "?column?":1!null
 ├── cardinality: [1 - 1]
 ├── key: ()
 ├── fd: ()-->(1)
 └── ('[]',)

# --------------------------------------------------
# FoldMultOne, FoldOneMult
# --------------------------------------------------

# Add columns to prevent NormalizeVar from swapping left and right.
norm expect=(FoldMultOne,FoldOneMult)
SELECT
    (a.i + a.i) * 1 AS r, 1 * (a.i + a.i) AS s,
    (a.f + a.f) * 1 AS t, 1 * (a.f + a.f) AS u,
    (a.d + a.d) * 1 AS v, 1 * (a.d + a.d) AS w
FROM a
----
project
 ├── columns: r:6 s:7 t:8 u:9 v:10 w:11
 ├── scan a
 │    └── columns: i:2 f:3 d:4
 └── projections
      ├── i + i [outer=(2)]
      ├── i + i [outer=(2)]
      ├── f + f [outer=(3)]
      ├── f + f [outer=(3)]
      ├── d + d [outer=(4)]
      └── d + d [outer=(4)]

# Regression test for #35113.
norm expect=FoldMultOne
SELECT i * 1::decimal FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

norm expect=FoldOneMult
SELECT 1::decimal * i FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

# --------------------------------------------------
# FoldDivOne
# --------------------------------------------------

norm expect=FoldDivOne
SELECT
    a.i / 1 AS r,
    a.f / 1 AS s,
    a.d / 1 AS t
FROM a
----
project
 ├── columns: r:6 s:7 t:8
 ├── scan a
 │    └── columns: i:2 f:3 d:4
 └── projections
      ├── i::DECIMAL [outer=(2)]
      ├── variable: f:3 [outer=(3)] [projected as s:7]
      └── variable: d:4 [outer=(4)] [projected as t:8]

# Regression test for #35113.
norm expect=FoldDivOne
SELECT i / 1::decimal FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

norm expect=FoldDivOne
SELECT i / 1::int8 FROM a
----
project
 ├── columns: "?column?":6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── i::DECIMAL [outer=(2)]

# --------------------------------------------------
# InvertMinus
# --------------------------------------------------
norm expect=InvertMinus
SELECT
    -(a.f - a.f) AS r,
    -(a.d - a.i) AS s,
    -(a.t - a.t) AS t
FROM a
----
project
 ├── columns: r:6 s:7 t:8
 ├── scan a
 │    └── columns: i:2 f:3 d:4 a.t:5
 └── projections
      ├── f - f [outer=(3)]
      ├── i - d [outer=(2,4)]
      └── a.t - a.t [outer=(5)]

# --------------------------------------------------
# EliminateUnaryMinus
# --------------------------------------------------
norm expect=EliminateUnaryMinus
SELECT -(-a.i::int) AS r FROM a
----
project
 ├── columns: r:6
 ├── scan a
 │    └── columns: i:2
 └── projections
      └── variable: i:2 [outer=(2)] [projected as r:6]
