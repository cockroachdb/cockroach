statement ok
CREATE TABLE t()

# Ensure old role must exist.
statement error pq: role/user "fake_old_role" does not exist
REASSIGN OWNED BY fake_old_role TO new_role

statement ok
CREATE ROLE old_role

# Ensure new role must exist.
statement error pq: role/user "fake_new_role" does not exist
REASSIGN OWNED BY old_role TO fake_new_role

# Ensure current user is member of both source/target roles

statement ok
CREATE ROLE new_role

statement ok
REASSIGN OWNED BY old_role TO new_role

# Can reassign from more than one old role to new role

# Confirm objects in all databases transferred

# Confirm tables are also reassigned

# Confirm schemas are also reassigned

# Confirm types are also reassigned

# Make sure operation is all or nothing if error is hit

# Focus on batched aspect of it

# Successfully drop user
