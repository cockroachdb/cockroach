statement ok
INSERT INTO system.users VALUES ('node', '', false);
GRANT node TO root;

# System tables can have their ranges split. Previously system tables
# with ID <= 10 could not be split.

statement ok
CREATE INDEX ON system.users ("username");
