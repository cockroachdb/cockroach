// Copyright 2023 The Cockroach Authors.
//
// Use of this software is governed by the CockroachDB Software License
// included in the /LICENSE file.

// @generated by protoc-gen-es v1.7.2 with parameter "target=js+dts"
// @generated from file multitenant/tenantcapabilitiespb/capabilities.proto (package cockroach.multitenant.tenantcapabilitiespb, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import type { BinaryReadOptions, FieldList, JsonReadOptions, JsonValue, PartialMessage, PlainMessage } from "@bufbuild/protobuf";
import { Message, proto3 } from "@bufbuild/protobuf";
import type { Constraint } from "../../roachpb/span_config_pb.js";

/**
 * TenantCapabilities encapsulates a set of capabilities[1] for a specific
 * tenant. Capabilities for a specific tenant are stored in the system.tenants
 * table and are checked against in KV when the tenant performs a privileged
 * operation.
 *
 * [1] Certain requests in the system are considered "privileged", and as such,
 * tenants are only allowed to perform them if they have the appropriate
 * capability. For example, performing an AdminSplit.
 *
 * When adding new capabilities to this list, please make sure to add support
 * for the new values to `show_tenant.go` and `tenant_capability.go` in order to
 * integrate them with the SQL infrastructure for managing capabilities.
 *
 * @generated from message cockroach.multitenant.tenantcapabilitiespb.TenantCapabilities
 */
export declare class TenantCapabilities extends Message<TenantCapabilities> {
  /**
   * DisableAdminSplit, if set to true, revokes the tenants ability to
   * successfully perform `AdminSplit` requests.
   *
   * This field uses the "disabled" verbiage, unlike the other fields on this
   * proto. Doing so ensures the zero value translates to `AdminSplits` being
   * allowed by default for secondary tenants. This is because splits and
   * scatters are integral to the performance of IMPORT/RESTORE.
   *
   * @generated from field: bool disable_admin_split = 1;
   */
  disableAdminSplit: boolean;

  /**
   * DisableAdminScatter, if set to true, revokes a tenant's the ability to
   * successfully perform `AdminScatter` requests.
   *
   * This field uses the "disabled" verbiage, unlike the other fields on this
   * proto. Doing so ensures the zero value translates to `AdminScatters` being
   * allowed by default for secondary tenants. This is because splits and
   * scatters are integral to the performance of IMPORT/RESTORE.
   *
   * @generated from field: bool disable_admin_scatter = 6;
   */
  disableAdminScatter: boolean;

  /**
   * CanViewNodeInfo, if set to true, grants the tenant the ability
   * retrieve node-level observability data at endpoints such as `_status/nodes`
   * and in the DB Console overview page.
   *
   * @generated from field: bool can_view_node_info = 2;
   */
  canViewNodeInfo: boolean;

  /**
   * CanViewTSDBMetrics, if set to true, grants the tenant the ability to
   * make arbitrary queries of the TSDB of the entire cluster. Currently,
   * we do not store per-tenant metrics so this will surface system metrics
   * to the tenant.
   * TODO(davidh): Revise this once tenant-scoped metrics are implemented in
   * https://github.com/cockroachdb/cockroach/issues/96438
   *
   * @generated from field: bool can_view_tsdb_metrics = 3;
   */
  canViewTsdbMetrics: boolean;

  /**
   * SpanConfigBounds, if set, bound the allowable values of span
   * configurations enacted by the tenant. Note that the tenant can install
   * span configs which do not conform to its bounds; the bounds result in
   * post-hoc clamping of the span configs to conform. This model diverges
   * from other capabilities because the span config infrastructure is
   * stateful and asynchronous. The bounds may change after a span config
   * has been accepted, in which case no error would flow to the user.
   * Furthermore the span config reconciliation infrastructure is poorly
   * positioned to surface errors to the user.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds span_config_bounds = 4;
   */
  spanConfigBounds?: SpanConfigBounds;

  /**
   * CanAdminUnsplit if, set to true, grants the tenant the ability to
   * successfully perform `AdminUnsplit` requests.
   *
   * @generated from field: bool can_admin_unsplit = 5;
   */
  canAdminUnsplit: boolean;

  /**
   * CanAdminRelocateRange if set to true, grants the tenant the ability to
   * successfully perform `AdminChangeReplicas`, `AdminRelocateRange`,
   * `AdminTransferLease` requests.
   *
   * @generated from field: bool can_admin_relocate_range = 7;
   */
  canAdminRelocateRange: boolean;

  /**
   * ExemptFromRateLimiting, if set to true, exempts the tenant from the KV-side tenant
   * rate limiter.
   *
   * @generated from field: bool exempt_from_rate_limiting = 8;
   */
  exemptFromRateLimiting: boolean;

  /**
   * CanUseNodelocalStorage if set to true, grants the tenant the ability
   * to use nodelocal storage  on any KV node.
   *
   * @generated from field: bool can_use_nodelocal_storage = 9;
   */
  canUseNodelocalStorage: boolean;

  /**
   * CanCheckConsistency if set to true, grants the tenant the ability to run
   * range consistency checks.
   *
   * @generated from field: bool can_check_consistency = 10;
   */
  canCheckConsistency: boolean;

  /**
   * CanDebugProcess, if set to true, grants the tenant the ability to
   * set vmodule on the process and run pprof profiles and tools.
   *
   * @generated from field: bool can_debug_process = 11;
   */
  canDebugProcess: boolean;

  /**
   * CanViewAllMetrics, if set to true, grants the tenant the ability
   * to query any metrics from the host.
   *
   * @generated from field: bool can_view_all_metrics = 12;
   */
  canViewAllMetrics: boolean;

  /**
   * CanPrepareTxns, if set to true, grants the tenant the ability to prepare
   * transactions as part of the XA two-phase commit protocol.
   *
   * @generated from field: bool can_prepare_txns = 13;
   */
  canPrepareTxns: boolean;

  constructor(data?: PartialMessage<TenantCapabilities>);

  static readonly runtime: typeof proto3;
  static readonly typeName = "cockroach.multitenant.tenantcapabilitiespb.TenantCapabilities";
  static readonly fields: FieldList;

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): TenantCapabilities;

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): TenantCapabilities;

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): TenantCapabilities;

  static equals(a: TenantCapabilities | PlainMessage<TenantCapabilities> | undefined, b: TenantCapabilities | PlainMessage<TenantCapabilities> | undefined): boolean;
}

/**
 * SpanConfigBound is used to constrain the possible values a SpanConfig may
 * contain.
 *
 * @generated from message cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds
 */
export declare class SpanConfigBounds extends Message<SpanConfigBounds> {
  /**
   * GcTtlSeconds bounds the configuration of gc.ttl_seconds.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int32Range gc_ttl_seconds = 1;
   */
  gcTtlSeconds?: SpanConfigBounds_Int32Range;

  /**
   * RangeMaxSize bounds the configuration of range_max_bytes.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int64Range range_max_bytes = 2;
   */
  rangeMaxBytes?: SpanConfigBounds_Int64Range;

  /**
   * RangeMinSize bounds the configuration of range_min_bytes.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int64Range range_min_bytes = 3;
   */
  rangeMinBytes?: SpanConfigBounds_Int64Range;

  /**
   * NumReplicas bounds the configuration of num_replicas.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int32Range num_replicas = 4;
   */
  numReplicas?: SpanConfigBounds_Int32Range;

  /**
   * NumVoters bounds the configuration of num_voters.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int32Range num_voters = 5;
   */
  numVoters?: SpanConfigBounds_Int32Range;

  /**
   * ConstraintBounds is used to bound the replication constraint fields of
   * a span config: constraints, voter_constraints, and
   * leaseholder_preferences.
   *
   * @generated from field: cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.ConstraintBounds constraint_bounds = 6;
   */
  constraintBounds?: SpanConfigBounds_ConstraintBounds;

  constructor(data?: PartialMessage<SpanConfigBounds>);

  static readonly runtime: typeof proto3;
  static readonly typeName = "cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds";
  static readonly fields: FieldList;

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): SpanConfigBounds;

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): SpanConfigBounds;

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): SpanConfigBounds;

  static equals(a: SpanConfigBounds | PlainMessage<SpanConfigBounds> | undefined, b: SpanConfigBounds | PlainMessage<SpanConfigBounds> | undefined): boolean;
}

/**
 * Int32Range is an interval of int32 representing [start, end].
 * If end is less than start, it is interpreted to be equal
 * start; there is no invalid representation.
 *
 * @generated from message cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int32Range
 */
export declare class SpanConfigBounds_Int32Range extends Message<SpanConfigBounds_Int32Range> {
  /**
   * @generated from field: int32 start = 1;
   */
  start: number;

  /**
   * @generated from field: int32 end = 2;
   */
  end: number;

  constructor(data?: PartialMessage<SpanConfigBounds_Int32Range>);

  static readonly runtime: typeof proto3;
  static readonly typeName = "cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int32Range";
  static readonly fields: FieldList;

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): SpanConfigBounds_Int32Range;

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): SpanConfigBounds_Int32Range;

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): SpanConfigBounds_Int32Range;

  static equals(a: SpanConfigBounds_Int32Range | PlainMessage<SpanConfigBounds_Int32Range> | undefined, b: SpanConfigBounds_Int32Range | PlainMessage<SpanConfigBounds_Int32Range> | undefined): boolean;
}

/**
 * Int64Range is an interval of int64 representing [start, end].
 * If end is less than start, it is interpreted to be equal
 * start; there is no invalid representation.
 *
 * @generated from message cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int64Range
 */
export declare class SpanConfigBounds_Int64Range extends Message<SpanConfigBounds_Int64Range> {
  /**
   * @generated from field: int64 start = 1;
   */
  start: bigint;

  /**
   * @generated from field: int64 end = 2;
   */
  end: bigint;

  constructor(data?: PartialMessage<SpanConfigBounds_Int64Range>);

  static readonly runtime: typeof proto3;
  static readonly typeName = "cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.Int64Range";
  static readonly fields: FieldList;

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): SpanConfigBounds_Int64Range;

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): SpanConfigBounds_Int64Range;

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): SpanConfigBounds_Int64Range;

  static equals(a: SpanConfigBounds_Int64Range | PlainMessage<SpanConfigBounds_Int64Range> | undefined, b: SpanConfigBounds_Int64Range | PlainMessage<SpanConfigBounds_Int64Range> | undefined): boolean;
}

/**
 * ConstraintsConjunction is like the roachpb equivalent with the same name,
 * but lacking a num_replicas field. This message is used when clamping a
 * SpanConfig constraint which is out of conformance.
 *
 * @generated from message cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.ConstraintsConjunction
 */
export declare class SpanConfigBounds_ConstraintsConjunction extends Message<SpanConfigBounds_ConstraintsConjunction> {
  /**
   * @generated from field: repeated cockroach.roachpb.Constraint constraints = 1;
   */
  constraints: Constraint[];

  constructor(data?: PartialMessage<SpanConfigBounds_ConstraintsConjunction>);

  static readonly runtime: typeof proto3;
  static readonly typeName = "cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.ConstraintsConjunction";
  static readonly fields: FieldList;

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): SpanConfigBounds_ConstraintsConjunction;

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): SpanConfigBounds_ConstraintsConjunction;

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): SpanConfigBounds_ConstraintsConjunction;

  static equals(a: SpanConfigBounds_ConstraintsConjunction | PlainMessage<SpanConfigBounds_ConstraintsConjunction> | undefined, b: SpanConfigBounds_ConstraintsConjunction | PlainMessage<SpanConfigBounds_ConstraintsConjunction> | undefined): boolean;
}

/**
 * ConstraintBounds represents bounds on voter_constraints, constraints,
 * and lease_preferences. These fields are bound by the same constraints,
 * because they need to be internally consistent.
 *
 * The basic structure is that these fields will be checked against
 * the allowed constraint bounds, and, if the values fall outside
 * those bounds, they will be clamped using the values in fallback.
 * See spanconfigbounds/doc.go for more discussion.
 *
 * Note: the fallback constraints should conform to the allowed constraints,
 * but if they do not, then a clamped configuration may not conform.
 *
 * TODO(ajwerner): Perhaps add a fallback rule on conformance to say that
 * a constraint conforms if its constraints exactly match fallback. If we
 * did that, then we would be able to say that a clamped constraint always
 * conforms and we wouldn't have any sort of invalid state.
 *
 * @generated from message cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.ConstraintBounds
 */
export declare class SpanConfigBounds_ConstraintBounds extends Message<SpanConfigBounds_ConstraintBounds> {
  /**
   * Constraints is a *sorted* slice of permitted constraints.
   *
   * If a constraint in a SpanConfig does not match a constraint in this
   * slice, the entire field is considered invalid and will be clamped.
   *
   * The logic for clamping is to mirror the structure of the existing
   * distribution of replicas, to the extent possible, but as applied to
   * the fallback constraints.
   *
   * @generated from field: repeated cockroach.roachpb.Constraint allowed = 1;
   */
  allowed: Constraint[];

  /**
   * FallbackConstraints are used to determine the constraints to use when
   * clamping a configuration which is out of conformance.
   *
   * If the existing constraint had just one entry with no numeric bound,
   * the first element will be used as the constraint.
   *
   * If the existing constraint had just one entry with a numeric constraint,
   * we'll clamp this to the num_replicas/2.
   *
   * If the existing constraint had more than one entry with numeric
   * constraints, we'll clamp accordingly.
   *
   * @generated from field: repeated cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.ConstraintsConjunction fallback = 2;
   */
  fallback: SpanConfigBounds_ConstraintsConjunction[];

  constructor(data?: PartialMessage<SpanConfigBounds_ConstraintBounds>);

  static readonly runtime: typeof proto3;
  static readonly typeName = "cockroach.multitenant.tenantcapabilitiespb.SpanConfigBounds.ConstraintBounds";
  static readonly fields: FieldList;

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): SpanConfigBounds_ConstraintBounds;

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): SpanConfigBounds_ConstraintBounds;

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): SpanConfigBounds_ConstraintBounds;

  static equals(a: SpanConfigBounds_ConstraintBounds | PlainMessage<SpanConfigBounds_ConstraintBounds> | undefined, b: SpanConfigBounds_ConstraintBounds | PlainMessage<SpanConfigBounds_ConstraintBounds> | undefined): boolean;
}

