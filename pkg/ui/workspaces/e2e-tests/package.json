{
  "name": "@cockroachlabs/e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for the CockroachDB UI",
  "license": "BSL",
  "private": true,
  "scripts": {
    "movr": "../../../../cockroach demo movr --multitenant=false",
    "test": "./build/start-crdb-movr.sh 'yarn cy:run'",
    "test:debug": "./build/start-crdb-movr.sh 'yarn cy:run'",
    "cy:run": "cypress run --e2e",
    "cy:debug": "cypress open --e2e --browser=chrome"
  },
  "devDependencies": {
    "@testing-library/cypress": "^8.0.2",
    "@types/node": "16",
    "cypress": "^10.3.0",
    "typescript": "^4.6.3"
  }
}
