{
  "name": "@cockroachlabs/e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for the CockroachDB UI",
  "license": "BSL",
  "private": true,
  "scripts": {
    "movr": "../../../../cockroach demo movr --multitenant=false",
    "test": "./build/start-crdb-movr.sh 'yarn cypress:run'",
    "test:debug": "./build/start-crdb-movr.sh 'yarn cypress:run'",
    "cypress:run": "cypress run --e2e",
    "cypress:debug": "cypress open --e2e --browser=chrome"
  },
  "devDependencies": {
    "@testing-library/cypress": "^8.0.2",
    "cypress": "^10.3.0",
    "start-server-and-test": "^1.14.0",
    "typescript": "^4.6.3"
  }
}
