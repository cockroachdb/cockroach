// Code generated by go-bindata.
// sources:
// build/css/app.css
// build/css/libs/nv.d3.min.css
// build/index.html
// build/js/app.js
// build/js/libs/d3.min.js
// build/js/libs/lodash.min.js
// build/js/libs/mithril.min.js
// build/js/libs/nv.d3.min.js
// DO NOT EDIT!

package ui

import (
	"fmt"
	"io/ioutil"
	"strings"
	"os"
	"path/filepath"
)

// bindataRead reads the given file from disk. It returns an error on failure.
func bindataRead(path, name string) ([]byte, error) {
	buf, err := ioutil.ReadFile(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset %s at %s: %v", name, path, err)
	}
	return buf, err
}

type asset struct {
	bytes []byte
	info  os.FileInfo
}

// buildCssAppCss reads file data from disk. It returns an error on failure.
func buildCssAppCss() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/css/app.css"
	name := "build/css/app.css"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildCssLibsNvD3MinCss reads file data from disk. It returns an error on failure.
func buildCssLibsNvD3MinCss() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/css/libs/nv.d3.min.css"
	name := "build/css/libs/nv.d3.min.css"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildIndexHtml reads file data from disk. It returns an error on failure.
func buildIndexHtml() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/index.html"
	name := "build/index.html"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildJsAppJs reads file data from disk. It returns an error on failure.
func buildJsAppJs() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/js/app.js"
	name := "build/js/app.js"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildJsLibsD3MinJs reads file data from disk. It returns an error on failure.
func buildJsLibsD3MinJs() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/js/libs/d3.min.js"
	name := "build/js/libs/d3.min.js"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildJsLibsLodashMinJs reads file data from disk. It returns an error on failure.
func buildJsLibsLodashMinJs() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/js/libs/lodash.min.js"
	name := "build/js/libs/lodash.min.js"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildJsLibsMithrilMinJs reads file data from disk. It returns an error on failure.
func buildJsLibsMithrilMinJs() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/js/libs/mithril.min.js"
	name := "build/js/libs/mithril.min.js"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// buildJsLibsNvD3MinJs reads file data from disk. It returns an error on failure.
func buildJsLibsNvD3MinJs() (*asset, error) {
	path := "/Users/maxlang/go/src/github.com/cockroachdb/cockroach/ui/build/js/libs/nv.d3.min.js"
	name := "build/js/libs/nv.d3.min.js"
	bytes, err := bindataRead(path, name)
	if err != nil {
		return nil, err
	}

	fi, err := os.Stat(path)
	if err != nil {
		err = fmt.Errorf("Error reading asset info %s at %s: %v", name, path, err)
	}

	a := &asset{bytes: bytes, info: fi}
	return a, err
}

// Asset loads and returns the asset for the given name.
// It returns an error if the asset could not be found or
// could not be loaded.
func Asset(name string) ([]byte, error) {
	cannonicalName := strings.Replace(name, "\\", "/", -1)
	if f, ok := _bindata[cannonicalName]; ok {
		a, err := f()
		if err != nil {
			return nil, fmt.Errorf("Asset %s can't read by error: %v", name, err)
		}
		return a.bytes, nil
	}
	return nil, fmt.Errorf("Asset %s not found", name)
}

// MustAsset is like Asset but panics when Asset would return an error.
// It simplifies safe initialization of global variables.
func MustAsset(name string) []byte {
	a, err := Asset(name)
	if (err != nil) {
		panic("asset: Asset(" + name + "): " + err.Error())
	}

	return a
}

// AssetInfo loads and returns the asset info for the given name.
// It returns an error if the asset could not be found or
// could not be loaded.
func AssetInfo(name string) (os.FileInfo, error) {
	cannonicalName := strings.Replace(name, "\\", "/", -1)
	if f, ok := _bindata[cannonicalName]; ok {
		a, err := f()
		if err != nil {
			return nil, fmt.Errorf("AssetInfo %s can't read by error: %v", name, err)
		}
		return a.info, nil
	}
	return nil, fmt.Errorf("AssetInfo %s not found", name)
}

// AssetNames returns the names of the assets.
func AssetNames() []string {
	names := make([]string, 0, len(_bindata))
	for name := range _bindata {
		names = append(names, name)
	}
	return names
}

// _bindata is a table, holding each asset generator, mapped to its name.
var _bindata = map[string]func() (*asset, error){
	"build/css/app.css": buildCssAppCss,
	"build/css/libs/nv.d3.min.css": buildCssLibsNvD3MinCss,
	"build/index.html": buildIndexHtml,
	"build/js/app.js": buildJsAppJs,
	"build/js/libs/d3.min.js": buildJsLibsD3MinJs,
	"build/js/libs/lodash.min.js": buildJsLibsLodashMinJs,
	"build/js/libs/mithril.min.js": buildJsLibsMithrilMinJs,
	"build/js/libs/nv.d3.min.js": buildJsLibsNvD3MinJs,
}

// AssetDir returns the file names below a certain
// directory embedded in the file by go-bindata.
// For example if you run go-bindata on data/... and data contains the
// following hierarchy:
//     data/
//       foo.txt
//       img/
//         a.png
//         b.png
// then AssetDir("data") would return []string{"foo.txt", "img"}
// AssetDir("data/img") would return []string{"a.png", "b.png"}
// AssetDir("foo.txt") and AssetDir("notexist") would return an error
// AssetDir("") will return []string{"data"}.
func AssetDir(name string) ([]string, error) {
	node := _bintree
	if len(name) != 0 {
		cannonicalName := strings.Replace(name, "\\", "/", -1)
		pathList := strings.Split(cannonicalName, "/")
		for _, p := range pathList {
			node = node.Children[p]
			if node == nil {
				return nil, fmt.Errorf("Asset %s not found", name)
			}
		}
	}
	if node.Func != nil {
		return nil, fmt.Errorf("Asset %s not found", name)
	}
	rv := make([]string, 0, len(node.Children))
	for childName := range node.Children {
		rv = append(rv, childName)
	}
	return rv, nil
}

type bintree struct {
	Func func() (*asset, error)
	Children map[string]*bintree
}
var _bintree = &bintree{nil, map[string]*bintree{
	"build": &bintree{nil, map[string]*bintree{
		"css": &bintree{nil, map[string]*bintree{
			"app.css": &bintree{buildCssAppCss, map[string]*bintree{
			}},
			"libs": &bintree{nil, map[string]*bintree{
				"nv.d3.min.css": &bintree{buildCssLibsNvD3MinCss, map[string]*bintree{
				}},
			}},
		}},
		"index.html": &bintree{buildIndexHtml, map[string]*bintree{
		}},
		"js": &bintree{nil, map[string]*bintree{
			"app.js": &bintree{buildJsAppJs, map[string]*bintree{
			}},
			"libs": &bintree{nil, map[string]*bintree{
				"d3.min.js": &bintree{buildJsLibsD3MinJs, map[string]*bintree{
				}},
				"lodash.min.js": &bintree{buildJsLibsLodashMinJs, map[string]*bintree{
				}},
				"mithril.min.js": &bintree{buildJsLibsMithrilMinJs, map[string]*bintree{
				}},
				"nv.d3.min.js": &bintree{buildJsLibsNvD3MinJs, map[string]*bintree{
				}},
			}},
		}},
	}},
}}

// RestoreAsset restores an asset under the given directory
func RestoreAsset(dir, name string) error {
        data, err := Asset(name)
        if err != nil {
                return err
        }
        info, err := AssetInfo(name)
        if err != nil {
                return err
        }
        err = os.MkdirAll(_filePath(dir, filepath.Dir(name)), os.FileMode(0755))
        if err != nil {
                return err
        }
        err = ioutil.WriteFile(_filePath(dir, name), data, info.Mode())
        if err != nil {
                return err
        }
        err = os.Chtimes(_filePath(dir, name), info.ModTime(), info.ModTime())
        if err != nil {
                return err
        }
        return nil
}

// RestoreAssets restores an asset under the given directory recursively
func RestoreAssets(dir, name string) error {
        children, err := AssetDir(name)
        // File
        if err != nil {
                return RestoreAsset(dir, name)
        }
        // Dir
        for _, child := range children {
                err = RestoreAssets(dir, filepath.Join(name, child))
                if err != nil {
                        return err
                }
        }
        return nil
}

func _filePath(dir, name string) string {
        cannonicalName := strings.Replace(name, "\\", "/", -1)
        return filepath.Join(append([]string{dir}, strings.Split(cannonicalName, "/")...)...)
}

